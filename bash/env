export GIT_EDITOR="vim"

if [[ "$HOME" == "/home/cocktail/held/" ]];
then
  source env_uni 
else
  source env_local
fi

HISTFILESIZE=1000000000 HISTSIZE=1000000
[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"  # This loads RVM into a shell session.
